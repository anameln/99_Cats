<% show_params = [:start_date, :end_date] %>

<%= request.requester.user_name %>
<% show_params.each do |param| %>
  <table>
    <tr>
      <th>
        <%= param %>
      </th>
      <td>
        <%= request.send(param) %>
      </td>
    </tr>
  </table>
<% end %>
